// build.gradle di Root
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Versi Gradle Plugin yang stabil
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
